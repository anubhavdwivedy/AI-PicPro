services:
  - type: web
    name: ai-image-toolkit
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: SECRET_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
